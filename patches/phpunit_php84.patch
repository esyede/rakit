--- a/src/Framework/Error.php
+++ b/src/Framework/Error.php
@@ -24,7 +24,10 @@
      * @param int       $line
      * @param Exception $previous
      */
-    public function __construct($message, $code, $file, $line, Exception $previous = null)
+    // Note: the 'Exception' type hint is intentionally left out. PHP 8.4 deprecates
+    // an implicitly nullable parameter, and the only fix for that ('?Exception')
+    // is a parse error before PHP 7.1 - this package still has to load on PHP 5.4.
+    public function __construct($message, $code, $file, $line, $previous = null)
     {
         parent::__construct($message, $code, $previous);
 
--- a/src/Framework/Exception.php
+++ b/src/Framework/Exception.php
@@ -36,7 +36,10 @@
      */
     protected $serializableTrace;
 
-    public function __construct($message = '', $code = 0, Exception $previous = null)
+    // Note: the 'Exception' type hint is intentionally left out. PHP 8.4 deprecates
+    // an implicitly nullable parameter, and the only fix for that ('?Exception')
+    // is a parse error before PHP 7.1 - this package still has to load on PHP 5.4.
+    public function __construct($message = '', $code = 0, $previous = null)
     {
         parent::__construct($message, $code, $previous);
 
--- a/src/Framework/TestResult.php
+++ b/src/Framework/TestResult.php
@@ -556,7 +556,7 @@
         if ($this->convertErrorsToExceptions) {
             $oldErrorHandler = set_error_handler(
                 array('PHPUnit_Util_ErrorHandler', 'handleError'),
-                E_ALL | E_STRICT
+                E_ALL | 2048
             );
 
             if ($oldErrorHandler === null) {
--- a/src/Util/ErrorHandler.php
+++ b/src/Util/ErrorHandler.php
@@ -60,7 +60,7 @@
             }
         }
 
-        if ($errno == E_NOTICE || $errno == E_USER_NOTICE || $errno == E_STRICT) {
+        if ($errno == E_NOTICE || $errno == E_USER_NOTICE || $errno == 2048) {
             if (PHPUnit_Framework_Error_Notice::$enabled !== true) {
                 return false;
             }
